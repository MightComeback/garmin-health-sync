# 2026-02-06

## whisper.cpp integration (urgent)

**Action**: Added local whisper.cpp transcription integration to this repo.

**Implementation**:
- Added `scripts/whispercpp/setup.sh` to clone/build whisper.cpp into `./.cache/whispercpp/repo` and download a model.
- Added `lib/whispercpp.ts` (TS wrapper) to run whisper.cpp via child_process and optionally convert audio via ffmpeg.
- Added `POST /transcribe` endpoint to `lib/server.ts` (accepts `audioBase64`, optional `ext`, `language`, `threads`, `modelPath`). Increased JSON body limit to 50mb.
- Updated `README.md` and added `package.json` script: `bun run whispercpp:setup`.

**Repo hygiene**:
- Used parallel git worktrees to implement in branches and merged back.
- Expanded `.gitignore` to ignore OpenClaw workspace/automation artifacts and `.cache/`.

**Notes**:
- `bun test` currently fails due to unrelated tests/fixtures in other folders; not caused by whisper.cpp changes.
